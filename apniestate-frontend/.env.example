# API Configuration
# URL of the backend API used in production (used if no proxy)
VITE_API_URL=https://api.apniestate.com/api

# Vite Development Proxy Target
# When running `npm run dev`, Vite proxies /api to this target
VITE_API_TARGET=http://localhost:3000
