# Production API Configuration
# Points directly to the deployed backend URL
VITE_API_URL=https://api.apniestate.com/api

# Proxy target is not used in production build, but documented here
VITE_API_TARGET=https://api.apniestate.com
